<%= form_for(@administrativo) do |f| %>
  <% if @administrativo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@administrativo.errors.count, "error") %> prohibited this administrativo from being saved:</h2>

      <ul>
      <% @administrativo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :pass %><br />
    <%= f.text_field :pass %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
